# Copilot Operating Instructions (Repo-Scoped)

Load and follow `AGENTS.md` at the start of every chat.

If a custom agent is selected, apply this layering model:
- Shared protocol: `AGENTS.md`
- Role-specific protocol: selected `.github/agents/*.agent.md`

When files conflict, prefer `AGENTS.md` for shared process and governance.
