# Environment secrets — never commit
.env
.env.local
.env.*.local

# Local project config (private identifiers)
.figma-config.local

# Node dependencies
node_modules/

# OS artifacts
.DS_Store
Thumbs.db
